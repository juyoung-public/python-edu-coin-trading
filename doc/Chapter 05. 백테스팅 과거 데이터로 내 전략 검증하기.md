# Chapter 05. 백테스팅: 과거 데이터로 내 전략 검증하기

데이터 시각화까지 마쳤다면, 이제 가장 중요한 질문에 답할 차례입니다. 
**"내가 만든 이 전략으로 실제 투자를 했다면, 정말 돈을 벌었을까?"**

---

## 1. 백테스팅(Backtesting)이란? 🧪

백테스팅은 **'과거(Back) + 시험(Testing)'**의 합성어로, 특정 매매 전략을 과거의 데이터에 대입하여 어떤 결과가 나왔을지 시뮬레이션하는 과정입니다.

*   **모의고사와 같습니다:** 수능을 보기 전에 기출문제를 풀어보며 내 실력을 점검하듯, 내 돈을 투자하기 전에 과거 시장 데이터에 내 전략을 적용해 보는 것입니다.
*   **감정을 배제합니다:** "이쯤이면 오르겠지?"라는 근거 없는 기대 대신, "과거 데이터상 이 전략은 70%의 승률을 보였다"라는 **통계적 근거**를 갖게 해줍니다.
*   **주의사항:** 과거에 수익이 났다고 해서 미래에도 반드시 수익이 난다는 보장은 없습니다. 하지만 **망할 확률이 높은 전략을 미리 걸러낼 수 있다는 것**만으로도 매우 가치 있는 작업입니다.

---

## 2. 우리가 사용할 전략: 변동성 돌파 전략 📈

세계적인 트레이더 '래리 윌리엄스'가 만든 아주 유명하고 심플한 전략입니다.

*   **원리:** 가격이 일정 수준 이상으로 강하게 치고 올라올 때(상승 에너지가 확인되었을 때) 빠르게 따라 들어가는 방식입니다.
*   **공식:**
    1.  **변동폭:** 전일 고가 - 전일 저가
    2.  **매수 목표가:** 당일 시가 + (전일 변동폭 * 0.5)
    3.  **매도:** 다음 날 시가에 무조건 매도 (하루 단위 매매)

---

## 3. 주피터 노트북에서 백테스팅 구현하기 💻

이제 `pandas`와 `numpy` 라이브러리를 이용해 비트코인 데이터를 계산해 보겠습니다.

### (1) 데이터 준비 및 목표가 계산
```python
import pybithumb
import pandas as pd
import numpy as np

# 1. 비트코인 일봉 데이터 가져오기
df = pybithumb.get_ohlcv("BTC")

# 2. 전일 변동폭 계산 (고가 - 저가)
# shift(1)은 데이터를 한 줄 아래로 미루어 '어제의 데이터'를 오늘 행에서 사용하게 합니다.
df['range'] = (df['high'] - df['low']).shift(1)

# 3. 매수 목표가 계산 (당일 시가 + 전일 변동폭 * 0.5)
df['target'] = df['open'] + df['range'] * 0.5

df.head() # 데이터 확인
```

### (2) 수익률(ROR) 계산하기
수익률(Rate of Return)은 `종가 / 매수가`로 계산합니다. 단, 고가가 목표가보다 높았을 때만 매수 조건이 성립됩니다.

```python
# 수익률(ROR) 계산
# np.where(조건, 참일때값, 거짓일때값)
# 당일 고가(high)가 목표가(target)보다 높으면 매수 성공 -> 수익률 = 종가 / 목표가
# 아니면 매수 안함 -> 수익률 = 1 (변화 없음)
df['ror'] = np.where(df['high'] > df['target'],
                     df['close'] / df['target'],
                     1)

# 거래 수수료 반영 (빗썸 수수료 약 0.04% 가정 시 0.9996을 곱함)
df['ror'] = df['ror'] * 0.9996 

print(f"최근 시황 수익률:\n{df['ror'].tail()}")
```

### (3) 누적 수익률(HPR) 계산 및 시각화
수익률을 계속 곱하면 내 자산이 어떻게 변했는지 알 수 있는 **누적 수익률(HPR)**이 됩니다.

```python
# 누적 수익률(Holding Period Return) - cumprod()는 누적 곱셈 함수입니다.
df['hpr'] = df['ror'].cumprod()

# 최종 누적 수익률 출력
final_return = (df['hpr'].iloc[-1] - 1) * 100
print(f"최종 누적 수익률: {final_return:.2f}%")

# 수익률 그래프 그리기
import matplotlib.pyplot as plt
df['hpr'].plot(figsize=(10, 6), title="Backtesting: Cumulative Return", color="blue")
plt.axhline(y=1, color='red', linestyle='--') # 원금 선 표시
plt.grid(True)
plt.show()
```

---

## 4. 결과 분석 및 토론 🗣️

그래프를 완성했다면 다음 내용을 확인해 보세요.

1.  **MDD (Maximum Drawdown):** 내 자산이 최고점에서 얼마나 깊게 떨어졌나요? 만약 수익률이 -30%까지 떨어지는 구간이 있다면, 여러분은 실제 투자에서 그 시기를 견딜 수 있을까요?
2.  **K값 최적화:** `target`을 계산할 때 곱했던 `0.5`를 `0.1`부터 `0.9`까지 바꿔보세요. 어떤 숫자가 가장 높은 수익을 주나요?
3.  **수수료의 무서움:** 수수료(`0.9996`)를 지우고 실행해 보세요. 수익률 차이가 얼마나 큰가요? 왜 잦은 매매가 위험한지 알 수 있습니다.

---

## ✅ 이번 챕터 요약 과제

1.  비트코인(BTC)이 아닌 **이더리움(ETH)**이나 **리플(XRP)** 데이터로 백테스팅을 진행해 보세요.
2.  가장 수익률이 좋았던 `K값`을 찾아서 기록해 보세요.
3.  백테스팅 결과를 캡처하여 자신의 학습 일지에 추가하세요.

---
**다음 장 예고:** (TODO)
